{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Gate/Approval Event (append-only, cryptographically bound in CI)",
  "type": "object",
  "required": ["event_type", "subject_id", "artifact_hash", "issuer", "role", "nonce", "issued_at", "expiry", "run_id"],
  "properties": {
    "event_type": { "enum": ["gate-verdict", "approval"] },
    "role": { "enum": ["reviewer", "owner", "legal", "privacy", "tax", "finance", "food-safety", "certification"] },
    "decision": { "enum": ["approved", "PASS", "FAIL", "BLOCKED"] },
    "author": { "type": "string", "description": "creator identity — must differ from issuer for high-risk approvals" },
    "signature": { "type": "string", "description": "populated by CI (GitHub Artifact Attestations / Sigstore)" },
    "transparency": { "type": "object", "description": "external anchor inclusion proof (Rekor / internal t-log)" }
  }
}
